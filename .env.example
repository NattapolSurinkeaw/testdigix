# ---------- MySQL ----------
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_PORT=3306

DB_NAME=book_library
DB_USER=digix
DB_PASSWORD=digixpass

# ---------- Backend ----------
JWT_SECRET=JWT_Token_Secret_Key
BACKEND_PORT=8000

# ---------- Frontend ----------
FRONTEND_PORT=5173
URL_FRONTEND=http://localhost:5173

# VITE_API_PATH ตอน dev: ใช้ localhost (browser เรียก backend ตรง ๆ)
# ตอน prod (ผ่าน nginx): ใช้ path เดียวกับ origin
VITE_API_PATH=http://localhost:8000/api
